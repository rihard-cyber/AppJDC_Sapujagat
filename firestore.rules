rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read/write for this app (no Firebase Auth — uses local PIN auth)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
